interface:
  display_name: "Git Commit Writer"
  short_description: "Write strict Conventional Commit messages"
  default_prompt: "Use $git-commit-writer to write a compliant commit message for the current changes."
